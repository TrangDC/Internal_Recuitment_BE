type Attachment {
  id: ID!
  document_name: String!
  document_id: ID!
}

input NewAttachmentInput {
  id: ID
  document_name: String!
  document_id: ID!
}

enum AttachmentRelationType {
  candidate_jobs
  candidate_job_feedbacks
  candidate_notes
}
