// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"trec/ent/candidateinterview"
	"trec/ent/candidateinterviewer"
	"trec/ent/predicate"
	"trec/ent/user"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// CandidateInterviewerQuery is the builder for querying CandidateInterviewer entities.
type CandidateInterviewerQuery struct {
	config
	limit             *int
	offset            *int
	unique            *bool
	order             []OrderFunc
	fields            []string
	predicates        []predicate.CandidateInterviewer
	withUserEdge      *UserQuery
	withInterviewEdge *CandidateInterviewQuery
	modifiers         []func(*sql.Selector)
	loadTotal         []func(context.Context, []*CandidateInterviewer) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CandidateInterviewerQuery builder.
func (ciq *CandidateInterviewerQuery) Where(ps ...predicate.CandidateInterviewer) *CandidateInterviewerQuery {
	ciq.predicates = append(ciq.predicates, ps...)
	return ciq
}

// Limit adds a limit step to the query.
func (ciq *CandidateInterviewerQuery) Limit(limit int) *CandidateInterviewerQuery {
	ciq.limit = &limit
	return ciq
}

// Offset adds an offset step to the query.
func (ciq *CandidateInterviewerQuery) Offset(offset int) *CandidateInterviewerQuery {
	ciq.offset = &offset
	return ciq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ciq *CandidateInterviewerQuery) Unique(unique bool) *CandidateInterviewerQuery {
	ciq.unique = &unique
	return ciq
}

// Order adds an order step to the query.
func (ciq *CandidateInterviewerQuery) Order(o ...OrderFunc) *CandidateInterviewerQuery {
	ciq.order = append(ciq.order, o...)
	return ciq
}

// QueryUserEdge chains the current query on the "user_edge" edge.
func (ciq *CandidateInterviewerQuery) QueryUserEdge() *UserQuery {
	query := &UserQuery{config: ciq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ciq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ciq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(candidateinterviewer.Table, candidateinterviewer.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, candidateinterviewer.UserEdgeTable, candidateinterviewer.UserEdgeColumn),
		)
		fromU = sqlgraph.SetNeighbors(ciq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryInterviewEdge chains the current query on the "interview_edge" edge.
func (ciq *CandidateInterviewerQuery) QueryInterviewEdge() *CandidateInterviewQuery {
	query := &CandidateInterviewQuery{config: ciq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ciq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ciq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(candidateinterviewer.Table, candidateinterviewer.FieldID, selector),
			sqlgraph.To(candidateinterview.Table, candidateinterview.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, candidateinterviewer.InterviewEdgeTable, candidateinterviewer.InterviewEdgeColumn),
		)
		fromU = sqlgraph.SetNeighbors(ciq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CandidateInterviewer entity from the query.
// Returns a *NotFoundError when no CandidateInterviewer was found.
func (ciq *CandidateInterviewerQuery) First(ctx context.Context) (*CandidateInterviewer, error) {
	nodes, err := ciq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{candidateinterviewer.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ciq *CandidateInterviewerQuery) FirstX(ctx context.Context) *CandidateInterviewer {
	node, err := ciq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CandidateInterviewer ID from the query.
// Returns a *NotFoundError when no CandidateInterviewer ID was found.
func (ciq *CandidateInterviewerQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ciq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{candidateinterviewer.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ciq *CandidateInterviewerQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := ciq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CandidateInterviewer entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CandidateInterviewer entity is found.
// Returns a *NotFoundError when no CandidateInterviewer entities are found.
func (ciq *CandidateInterviewerQuery) Only(ctx context.Context) (*CandidateInterviewer, error) {
	nodes, err := ciq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{candidateinterviewer.Label}
	default:
		return nil, &NotSingularError{candidateinterviewer.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ciq *CandidateInterviewerQuery) OnlyX(ctx context.Context) *CandidateInterviewer {
	node, err := ciq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CandidateInterviewer ID in the query.
// Returns a *NotSingularError when more than one CandidateInterviewer ID is found.
// Returns a *NotFoundError when no entities are found.
func (ciq *CandidateInterviewerQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ciq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{candidateinterviewer.Label}
	default:
		err = &NotSingularError{candidateinterviewer.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ciq *CandidateInterviewerQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := ciq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CandidateInterviewers.
func (ciq *CandidateInterviewerQuery) All(ctx context.Context) ([]*CandidateInterviewer, error) {
	if err := ciq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return ciq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (ciq *CandidateInterviewerQuery) AllX(ctx context.Context) []*CandidateInterviewer {
	nodes, err := ciq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CandidateInterviewer IDs.
func (ciq *CandidateInterviewerQuery) IDs(ctx context.Context) ([]uuid.UUID, error) {
	var ids []uuid.UUID
	if err := ciq.Select(candidateinterviewer.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ciq *CandidateInterviewerQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := ciq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ciq *CandidateInterviewerQuery) Count(ctx context.Context) (int, error) {
	if err := ciq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return ciq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (ciq *CandidateInterviewerQuery) CountX(ctx context.Context) int {
	count, err := ciq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ciq *CandidateInterviewerQuery) Exist(ctx context.Context) (bool, error) {
	if err := ciq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return ciq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (ciq *CandidateInterviewerQuery) ExistX(ctx context.Context) bool {
	exist, err := ciq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CandidateInterviewerQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ciq *CandidateInterviewerQuery) Clone() *CandidateInterviewerQuery {
	if ciq == nil {
		return nil
	}
	return &CandidateInterviewerQuery{
		config:            ciq.config,
		limit:             ciq.limit,
		offset:            ciq.offset,
		order:             append([]OrderFunc{}, ciq.order...),
		predicates:        append([]predicate.CandidateInterviewer{}, ciq.predicates...),
		withUserEdge:      ciq.withUserEdge.Clone(),
		withInterviewEdge: ciq.withInterviewEdge.Clone(),
		// clone intermediate query.
		sql:    ciq.sql.Clone(),
		path:   ciq.path,
		unique: ciq.unique,
	}
}

// WithUserEdge tells the query-builder to eager-load the nodes that are connected to
// the "user_edge" edge. The optional arguments are used to configure the query builder of the edge.
func (ciq *CandidateInterviewerQuery) WithUserEdge(opts ...func(*UserQuery)) *CandidateInterviewerQuery {
	query := &UserQuery{config: ciq.config}
	for _, opt := range opts {
		opt(query)
	}
	ciq.withUserEdge = query
	return ciq
}

// WithInterviewEdge tells the query-builder to eager-load the nodes that are connected to
// the "interview_edge" edge. The optional arguments are used to configure the query builder of the edge.
func (ciq *CandidateInterviewerQuery) WithInterviewEdge(opts ...func(*CandidateInterviewQuery)) *CandidateInterviewerQuery {
	query := &CandidateInterviewQuery{config: ciq.config}
	for _, opt := range opts {
		opt(query)
	}
	ciq.withInterviewEdge = query
	return ciq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CandidateInterviewer.Query().
//		GroupBy(candidateinterviewer.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ciq *CandidateInterviewerQuery) GroupBy(field string, fields ...string) *CandidateInterviewerGroupBy {
	grbuild := &CandidateInterviewerGroupBy{config: ciq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := ciq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return ciq.sqlQuery(ctx), nil
	}
	grbuild.label = candidateinterviewer.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.CandidateInterviewer.Query().
//		Select(candidateinterviewer.FieldCreatedAt).
//		Scan(ctx, &v)
func (ciq *CandidateInterviewerQuery) Select(fields ...string) *CandidateInterviewerSelect {
	ciq.fields = append(ciq.fields, fields...)
	selbuild := &CandidateInterviewerSelect{CandidateInterviewerQuery: ciq}
	selbuild.label = candidateinterviewer.Label
	selbuild.flds, selbuild.scan = &ciq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a CandidateInterviewerSelect configured with the given aggregations.
func (ciq *CandidateInterviewerQuery) Aggregate(fns ...AggregateFunc) *CandidateInterviewerSelect {
	return ciq.Select().Aggregate(fns...)
}

func (ciq *CandidateInterviewerQuery) prepareQuery(ctx context.Context) error {
	for _, f := range ciq.fields {
		if !candidateinterviewer.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ciq.path != nil {
		prev, err := ciq.path(ctx)
		if err != nil {
			return err
		}
		ciq.sql = prev
	}
	return nil
}

func (ciq *CandidateInterviewerQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CandidateInterviewer, error) {
	var (
		nodes       = []*CandidateInterviewer{}
		_spec       = ciq.querySpec()
		loadedTypes = [2]bool{
			ciq.withUserEdge != nil,
			ciq.withInterviewEdge != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CandidateInterviewer).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CandidateInterviewer{config: ciq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(ciq.modifiers) > 0 {
		_spec.Modifiers = ciq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ciq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ciq.withUserEdge; query != nil {
		if err := ciq.loadUserEdge(ctx, query, nodes, nil,
			func(n *CandidateInterviewer, e *User) { n.Edges.UserEdge = e }); err != nil {
			return nil, err
		}
	}
	if query := ciq.withInterviewEdge; query != nil {
		if err := ciq.loadInterviewEdge(ctx, query, nodes, nil,
			func(n *CandidateInterviewer, e *CandidateInterview) { n.Edges.InterviewEdge = e }); err != nil {
			return nil, err
		}
	}
	for i := range ciq.loadTotal {
		if err := ciq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ciq *CandidateInterviewerQuery) loadUserEdge(ctx context.Context, query *UserQuery, nodes []*CandidateInterviewer, init func(*CandidateInterviewer), assign func(*CandidateInterviewer, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*CandidateInterviewer)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (ciq *CandidateInterviewerQuery) loadInterviewEdge(ctx context.Context, query *CandidateInterviewQuery, nodes []*CandidateInterviewer, init func(*CandidateInterviewer), assign func(*CandidateInterviewer, *CandidateInterview)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*CandidateInterviewer)
	for i := range nodes {
		fk := nodes[i].CandidateInterviewID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	query.Where(candidateinterview.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "candidate_interview_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ciq *CandidateInterviewerQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ciq.querySpec()
	if len(ciq.modifiers) > 0 {
		_spec.Modifiers = ciq.modifiers
	}
	_spec.Node.Columns = ciq.fields
	if len(ciq.fields) > 0 {
		_spec.Unique = ciq.unique != nil && *ciq.unique
	}
	return sqlgraph.CountNodes(ctx, ciq.driver, _spec)
}

func (ciq *CandidateInterviewerQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := ciq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

func (ciq *CandidateInterviewerQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   candidateinterviewer.Table,
			Columns: candidateinterviewer.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: candidateinterviewer.FieldID,
			},
		},
		From:   ciq.sql,
		Unique: true,
	}
	if unique := ciq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := ciq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, candidateinterviewer.FieldID)
		for i := range fields {
			if fields[i] != candidateinterviewer.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ciq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ciq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ciq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ciq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ciq *CandidateInterviewerQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ciq.driver.Dialect())
	t1 := builder.Table(candidateinterviewer.Table)
	columns := ciq.fields
	if len(columns) == 0 {
		columns = candidateinterviewer.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ciq.sql != nil {
		selector = ciq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ciq.unique != nil && *ciq.unique {
		selector.Distinct()
	}
	for _, p := range ciq.predicates {
		p(selector)
	}
	for _, p := range ciq.order {
		p(selector)
	}
	if offset := ciq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ciq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CandidateInterviewerGroupBy is the group-by builder for CandidateInterviewer entities.
type CandidateInterviewerGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cigb *CandidateInterviewerGroupBy) Aggregate(fns ...AggregateFunc) *CandidateInterviewerGroupBy {
	cigb.fns = append(cigb.fns, fns...)
	return cigb
}

// Scan applies the group-by query and scans the result into the given value.
func (cigb *CandidateInterviewerGroupBy) Scan(ctx context.Context, v any) error {
	query, err := cigb.path(ctx)
	if err != nil {
		return err
	}
	cigb.sql = query
	return cigb.sqlScan(ctx, v)
}

func (cigb *CandidateInterviewerGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range cigb.fields {
		if !candidateinterviewer.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := cigb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cigb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (cigb *CandidateInterviewerGroupBy) sqlQuery() *sql.Selector {
	selector := cigb.sql.Select()
	aggregation := make([]string, 0, len(cigb.fns))
	for _, fn := range cigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(cigb.fields)+len(cigb.fns))
		for _, f := range cigb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(cigb.fields...)...)
}

// CandidateInterviewerSelect is the builder for selecting fields of CandidateInterviewer entities.
type CandidateInterviewerSelect struct {
	*CandidateInterviewerQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cis *CandidateInterviewerSelect) Aggregate(fns ...AggregateFunc) *CandidateInterviewerSelect {
	cis.fns = append(cis.fns, fns...)
	return cis
}

// Scan applies the selector query and scans the result into the given value.
func (cis *CandidateInterviewerSelect) Scan(ctx context.Context, v any) error {
	if err := cis.prepareQuery(ctx); err != nil {
		return err
	}
	cis.sql = cis.CandidateInterviewerQuery.sqlQuery(ctx)
	return cis.sqlScan(ctx, v)
}

func (cis *CandidateInterviewerSelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(cis.fns))
	for _, fn := range cis.fns {
		aggregation = append(aggregation, fn(cis.sql))
	}
	switch n := len(*cis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		cis.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		cis.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := cis.sql.Query()
	if err := cis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
