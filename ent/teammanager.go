// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"
	"trec/ent/team"
	"trec/ent/teammanager"
	"trec/ent/user"

	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// TeamManager is the model entity for the TeamManager schema.
type TeamManager struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt time.Time `json:"deleted_at,omitempty"`
	// TeamID holds the value of the "team_id" field.
	TeamID uuid.UUID `json:"team_id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID uuid.UUID `json:"user_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TeamManagerQuery when eager-loading is set.
	Edges TeamManagerEdges `json:"edges"`
}

// TeamManagerEdges holds the relations/edges for other nodes in the graph.
type TeamManagerEdges struct {
	// UserEdge holds the value of the user_edge edge.
	UserEdge *User `json:"user_edge,omitempty"`
	// TeamEdge holds the value of the team_edge edge.
	TeamEdge *Team `json:"team_edge,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
	// totalCount holds the count of the edges above.
	totalCount [2]map[string]int
}

// UserEdgeOrErr returns the UserEdge value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TeamManagerEdges) UserEdgeOrErr() (*User, error) {
	if e.loadedTypes[0] {
		if e.UserEdge == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.UserEdge, nil
	}
	return nil, &NotLoadedError{edge: "user_edge"}
}

// TeamEdgeOrErr returns the TeamEdge value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TeamManagerEdges) TeamEdgeOrErr() (*Team, error) {
	if e.loadedTypes[1] {
		if e.TeamEdge == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: team.Label}
		}
		return e.TeamEdge, nil
	}
	return nil, &NotLoadedError{edge: "team_edge"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TeamManager) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case teammanager.FieldCreatedAt, teammanager.FieldUpdatedAt, teammanager.FieldDeletedAt:
			values[i] = new(sql.NullTime)
		case teammanager.FieldID, teammanager.FieldTeamID, teammanager.FieldUserID:
			values[i] = new(uuid.UUID)
		default:
			return nil, fmt.Errorf("unexpected column %q for type TeamManager", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TeamManager fields.
func (tm *TeamManager) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case teammanager.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				tm.ID = *value
			}
		case teammanager.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				tm.CreatedAt = value.Time
			}
		case teammanager.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				tm.UpdatedAt = value.Time
			}
		case teammanager.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				tm.DeletedAt = value.Time
			}
		case teammanager.FieldTeamID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field team_id", values[i])
			} else if value != nil {
				tm.TeamID = *value
			}
		case teammanager.FieldUserID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value != nil {
				tm.UserID = *value
			}
		}
	}
	return nil
}

// QueryUserEdge queries the "user_edge" edge of the TeamManager entity.
func (tm *TeamManager) QueryUserEdge() *UserQuery {
	return (&TeamManagerClient{config: tm.config}).QueryUserEdge(tm)
}

// QueryTeamEdge queries the "team_edge" edge of the TeamManager entity.
func (tm *TeamManager) QueryTeamEdge() *TeamQuery {
	return (&TeamManagerClient{config: tm.config}).QueryTeamEdge(tm)
}

// Update returns a builder for updating this TeamManager.
// Note that you need to call TeamManager.Unwrap() before calling this method if this TeamManager
// was returned from a transaction, and the transaction was committed or rolled back.
func (tm *TeamManager) Update() *TeamManagerUpdateOne {
	return (&TeamManagerClient{config: tm.config}).UpdateOne(tm)
}

// Unwrap unwraps the TeamManager entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (tm *TeamManager) Unwrap() *TeamManager {
	_tx, ok := tm.config.driver.(*txDriver)
	if !ok {
		panic("ent: TeamManager is not a transactional entity")
	}
	tm.config.driver = _tx.drv
	return tm
}

// String implements the fmt.Stringer.
func (tm *TeamManager) String() string {
	var builder strings.Builder
	builder.WriteString("TeamManager(")
	builder.WriteString(fmt.Sprintf("id=%v, ", tm.ID))
	builder.WriteString("created_at=")
	builder.WriteString(tm.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(tm.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("deleted_at=")
	builder.WriteString(tm.DeletedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("team_id=")
	builder.WriteString(fmt.Sprintf("%v", tm.TeamID))
	builder.WriteString(", ")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", tm.UserID))
	builder.WriteByte(')')
	return builder.String()
}

// TeamManagers is a parsable slice of TeamManager.
type TeamManagers []*TeamManager

func (tm TeamManagers) config(cfg config) {
	for _i := range tm {
		tm[_i].config = cfg
	}
}
