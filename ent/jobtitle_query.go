// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"trec/ent/jobtitle"
	"trec/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// JobTitleQuery is the builder for querying JobTitle entities.
type JobTitleQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.JobTitle
	modifiers  []func(*sql.Selector)
	loadTotal  []func(context.Context, []*JobTitle) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the JobTitleQuery builder.
func (jtq *JobTitleQuery) Where(ps ...predicate.JobTitle) *JobTitleQuery {
	jtq.predicates = append(jtq.predicates, ps...)
	return jtq
}

// Limit adds a limit step to the query.
func (jtq *JobTitleQuery) Limit(limit int) *JobTitleQuery {
	jtq.limit = &limit
	return jtq
}

// Offset adds an offset step to the query.
func (jtq *JobTitleQuery) Offset(offset int) *JobTitleQuery {
	jtq.offset = &offset
	return jtq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (jtq *JobTitleQuery) Unique(unique bool) *JobTitleQuery {
	jtq.unique = &unique
	return jtq
}

// Order adds an order step to the query.
func (jtq *JobTitleQuery) Order(o ...OrderFunc) *JobTitleQuery {
	jtq.order = append(jtq.order, o...)
	return jtq
}

// First returns the first JobTitle entity from the query.
// Returns a *NotFoundError when no JobTitle was found.
func (jtq *JobTitleQuery) First(ctx context.Context) (*JobTitle, error) {
	nodes, err := jtq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{jobtitle.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (jtq *JobTitleQuery) FirstX(ctx context.Context) *JobTitle {
	node, err := jtq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first JobTitle ID from the query.
// Returns a *NotFoundError when no JobTitle ID was found.
func (jtq *JobTitleQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = jtq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{jobtitle.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (jtq *JobTitleQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := jtq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single JobTitle entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one JobTitle entity is found.
// Returns a *NotFoundError when no JobTitle entities are found.
func (jtq *JobTitleQuery) Only(ctx context.Context) (*JobTitle, error) {
	nodes, err := jtq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{jobtitle.Label}
	default:
		return nil, &NotSingularError{jobtitle.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (jtq *JobTitleQuery) OnlyX(ctx context.Context) *JobTitle {
	node, err := jtq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only JobTitle ID in the query.
// Returns a *NotSingularError when more than one JobTitle ID is found.
// Returns a *NotFoundError when no entities are found.
func (jtq *JobTitleQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = jtq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{jobtitle.Label}
	default:
		err = &NotSingularError{jobtitle.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (jtq *JobTitleQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := jtq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of JobTitles.
func (jtq *JobTitleQuery) All(ctx context.Context) ([]*JobTitle, error) {
	if err := jtq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return jtq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (jtq *JobTitleQuery) AllX(ctx context.Context) []*JobTitle {
	nodes, err := jtq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of JobTitle IDs.
func (jtq *JobTitleQuery) IDs(ctx context.Context) ([]uuid.UUID, error) {
	var ids []uuid.UUID
	if err := jtq.Select(jobtitle.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (jtq *JobTitleQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := jtq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (jtq *JobTitleQuery) Count(ctx context.Context) (int, error) {
	if err := jtq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return jtq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (jtq *JobTitleQuery) CountX(ctx context.Context) int {
	count, err := jtq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (jtq *JobTitleQuery) Exist(ctx context.Context) (bool, error) {
	if err := jtq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return jtq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (jtq *JobTitleQuery) ExistX(ctx context.Context) bool {
	exist, err := jtq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the JobTitleQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (jtq *JobTitleQuery) Clone() *JobTitleQuery {
	if jtq == nil {
		return nil
	}
	return &JobTitleQuery{
		config:     jtq.config,
		limit:      jtq.limit,
		offset:     jtq.offset,
		order:      append([]OrderFunc{}, jtq.order...),
		predicates: append([]predicate.JobTitle{}, jtq.predicates...),
		// clone intermediate query.
		sql:    jtq.sql.Clone(),
		path:   jtq.path,
		unique: jtq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.JobTitle.Query().
//		GroupBy(jobtitle.FieldCode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (jtq *JobTitleQuery) GroupBy(field string, fields ...string) *JobTitleGroupBy {
	grbuild := &JobTitleGroupBy{config: jtq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := jtq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return jtq.sqlQuery(ctx), nil
	}
	grbuild.label = jobtitle.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code,omitempty"`
//	}
//
//	client.JobTitle.Query().
//		Select(jobtitle.FieldCode).
//		Scan(ctx, &v)
func (jtq *JobTitleQuery) Select(fields ...string) *JobTitleSelect {
	jtq.fields = append(jtq.fields, fields...)
	selbuild := &JobTitleSelect{JobTitleQuery: jtq}
	selbuild.label = jobtitle.Label
	selbuild.flds, selbuild.scan = &jtq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a JobTitleSelect configured with the given aggregations.
func (jtq *JobTitleQuery) Aggregate(fns ...AggregateFunc) *JobTitleSelect {
	return jtq.Select().Aggregate(fns...)
}

func (jtq *JobTitleQuery) prepareQuery(ctx context.Context) error {
	for _, f := range jtq.fields {
		if !jobtitle.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if jtq.path != nil {
		prev, err := jtq.path(ctx)
		if err != nil {
			return err
		}
		jtq.sql = prev
	}
	return nil
}

func (jtq *JobTitleQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*JobTitle, error) {
	var (
		nodes = []*JobTitle{}
		_spec = jtq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*JobTitle).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &JobTitle{config: jtq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(jtq.modifiers) > 0 {
		_spec.Modifiers = jtq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, jtq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	for i := range jtq.loadTotal {
		if err := jtq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (jtq *JobTitleQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := jtq.querySpec()
	if len(jtq.modifiers) > 0 {
		_spec.Modifiers = jtq.modifiers
	}
	_spec.Node.Columns = jtq.fields
	if len(jtq.fields) > 0 {
		_spec.Unique = jtq.unique != nil && *jtq.unique
	}
	return sqlgraph.CountNodes(ctx, jtq.driver, _spec)
}

func (jtq *JobTitleQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := jtq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

func (jtq *JobTitleQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   jobtitle.Table,
			Columns: jobtitle.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: jobtitle.FieldID,
			},
		},
		From:   jtq.sql,
		Unique: true,
	}
	if unique := jtq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := jtq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, jobtitle.FieldID)
		for i := range fields {
			if fields[i] != jobtitle.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := jtq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := jtq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := jtq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := jtq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (jtq *JobTitleQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(jtq.driver.Dialect())
	t1 := builder.Table(jobtitle.Table)
	columns := jtq.fields
	if len(columns) == 0 {
		columns = jobtitle.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if jtq.sql != nil {
		selector = jtq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if jtq.unique != nil && *jtq.unique {
		selector.Distinct()
	}
	for _, p := range jtq.predicates {
		p(selector)
	}
	for _, p := range jtq.order {
		p(selector)
	}
	if offset := jtq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := jtq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// JobTitleGroupBy is the group-by builder for JobTitle entities.
type JobTitleGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (jtgb *JobTitleGroupBy) Aggregate(fns ...AggregateFunc) *JobTitleGroupBy {
	jtgb.fns = append(jtgb.fns, fns...)
	return jtgb
}

// Scan applies the group-by query and scans the result into the given value.
func (jtgb *JobTitleGroupBy) Scan(ctx context.Context, v any) error {
	query, err := jtgb.path(ctx)
	if err != nil {
		return err
	}
	jtgb.sql = query
	return jtgb.sqlScan(ctx, v)
}

func (jtgb *JobTitleGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range jtgb.fields {
		if !jobtitle.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := jtgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := jtgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (jtgb *JobTitleGroupBy) sqlQuery() *sql.Selector {
	selector := jtgb.sql.Select()
	aggregation := make([]string, 0, len(jtgb.fns))
	for _, fn := range jtgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(jtgb.fields)+len(jtgb.fns))
		for _, f := range jtgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(jtgb.fields...)...)
}

// JobTitleSelect is the builder for selecting fields of JobTitle entities.
type JobTitleSelect struct {
	*JobTitleQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (jts *JobTitleSelect) Aggregate(fns ...AggregateFunc) *JobTitleSelect {
	jts.fns = append(jts.fns, fns...)
	return jts
}

// Scan applies the selector query and scans the result into the given value.
func (jts *JobTitleSelect) Scan(ctx context.Context, v any) error {
	if err := jts.prepareQuery(ctx); err != nil {
		return err
	}
	jts.sql = jts.JobTitleQuery.sqlQuery(ctx)
	return jts.sqlScan(ctx, v)
}

func (jts *JobTitleSelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(jts.fns))
	for _, fn := range jts.fns {
		aggregation = append(aggregation, fn(jts.sql))
	}
	switch n := len(*jts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		jts.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		jts.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := jts.sql.Query()
	if err := jts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
